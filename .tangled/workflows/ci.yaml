when:
  - event: ["push", "manual"]
    branch: ["master"]
engine: "nixery"
dependencies:
  nixpkgs:
    - cargo
    - gcc
steps:
  - name: "Check"
    command: "cargo check"
  - name: "Build"
    command: "cargo build"
  - name: "cargo test"
    command: "cargo test"
